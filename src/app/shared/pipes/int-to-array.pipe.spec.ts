import { IntToArrayPipe } from './int-to-array.pipe';

describe('IntToArrayPipe', () => {
  it('create an instance', () => {
    const pipe = new IntToArrayPipe();
    expect(pipe).toBeTruthy();
  });
});
